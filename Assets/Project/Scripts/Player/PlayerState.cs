namespace Player
{
    /// <summary>
    /// Defines the two possible player states.
    /// </summary>
    public enum PlayerState
    {
        Yin,
        Yang
    }

    public enum ControlState
    {
        GamePlay,
        Cutscene,
        Dead
    }
}
